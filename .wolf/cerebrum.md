# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-05-27

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

## Key Learnings

- **Project:** LeylineSync
- **Description:** Realtime Magic: The Gathering style board/controller app built with Next.js and Supabase.
- **Card behavior resolution chain:** runtime reads behavior as `coalesce(game_cards.copied_script, cards.script)` across ~10 SQL functions (casting, rebuild_scripted_continuous_effects, activate_ability, combat keyword registration). `copied_script` = per-instance copy override; `cards.script` = base.
- **The Scryfall importer never writes `cards.script`** (scripts/import-scryfall-cards.mjs `mapScryfallCardToRow` omits it) and upserts `onConflict: 'id'`, so `ON CONFLICT DO UPDATE` leaves `cards.script` untouched → authored behavior survives reimports. This is why Phase 2 authored directly onto `cards.script` instead of building a separate oracle_id-keyed override table.
- **`cards.id` is the Scryfall printing id, not oracle identity.** `oracle_id` (added migration 075) is the card identity shared across printings; importer now persists it. Use `relink_card_scripts()` to re-attach a script if the representative printing for an oracle changes between imports.
- **State-based actions:** 0-toughness death (rule 704.5f) ignores indestructible; lethal marked damage (704.5g) is prevented by indestructible. Both handled in `move_lethal_damaged_creatures_to_graveyard` (migration 074).
- **Triggered abilities (migrations 076–077):** events detected by DB row triggers (mirrors the `cease_token` pattern). `fire_zone_change_triggers` on `game_cards` handles ETB (zone→battlefield) and dies (battlefield→graveyard); `fire_upkeep_triggers` on `game_turn_state` step→upkeep; `fire_attack_triggers` on `game_combat_assignments` insert. Each match calls `fire_card_triggers` → `enqueue_triggered_ability`, which inserts a `triggered_ability` stack item with effects + controller + source card name (`label`) baked into the payload (so resolution + display survive the source leaving). `resolve_top_of_stack` applies untargeted effects (gain_life/lose_life/deal_damage to controller|each_opponent, draw). Zone-change trigger name `trg_a_fire_zone_change` sorts before `trg_cease_token_off_battlefield` so a token's dies trigger enqueues before cease-to-exist; `game_stack_items.source_card_id` is ON DELETE SET NULL so the stack item survives, and `get_stack_items` coalesces `source_card.name` with the payload `label`. **Only targeted triggers remain unbuilt.**
- **`get_stack_items` was redefined in migrations 030→032→033**; the live version (033) has extra `target_player_id`/`target_username` columns. When reproducing it, base on 033, not 030, or you silently drop columns the client `StackItem` type depends on.
- **Triggered-ability effects live in `apply_triggered_ability_effects` (migration 078)** — a helper `resolve_top_of_stack` calls for the `triggered_ability` branch. To add a new auto-resolved trigger effect, edit this one function (no longer reproduce the whole resolve_top_of_stack). Supported: gain_life, lose_life, deal_damage (recipient controller|each_opponent), draw, create_token (by token name from seeded catalog), add_counters (+1/+1 on source). create_token inserts battlefield game_cards which itself fires the ETB zone-change trigger — intended.
- **Card behavior authoring stack:** `/cards/behavior` editor has Form mode (guided builder, `components/CardBehaviorForm.tsx` + model/codec in `lib/game/card-behavior-builder.ts`), JSON mode, and ✨ Generate-with-AI. The builder's vocabulary constants are the single source of truth: the form dropdowns, the LLM prompt (`lib/game/card-behavior-llm.ts` → `buildBehaviorAuthoringGuide`), and validation (`validateCardScript`) all derive from / agree on them. To add a new effect type, extend the builder + the Zod schema; the form and AI follow. AI route: `app/api/cards/generate-behavior/route.ts` — auth-gated, `claude-opus-4-8`, adaptive thinking, cached system prompt, validates output with `validateCardScript` and retries once; guarded by `ANTHROPIC_API_KEY` (returns 501 if unset). SDK: `@anthropic-ai/sdk`.
- **Reusable internal effect helpers:** `draw_card`/`adjust_player_life` RPCs gate on `auth.uid()` so they can't be reused for an arbitrary trigger controller; triggered-ability resolution applies life/draw inline against the payload's `controller_player_id` instead.
- **Spell effect types via the stack (migration 079):** `spell_effect.actions` types map to stack action types in `getSpellPlan` (ControllerListV4): `deal_damage`→deal_damage_creature/player, `pump`→pump_creature, `destroy/bounce/tap/untap`→`*_creature` (kind `creature_effect`, one shared V4 picker via `CREATURE_EFFECT_MAP`), `draw`→`draw_cards` (untargeted, kind `draw`, cast button calls onDrawCards not onCastCard). `draw_cards` resolution reuses `apply_triggered_ability_effects` with a synthetic `{type:draw}` effect (pass null source). destroy→owner's graveyard (fires `dies` triggers, intended), bounce→owner's hand (does NOT fire dies — that's only battlefield→graveyard); both reset is_tapped/damage/counters and controller→owner. **Gotcha:** `doesCardRequireStackTarget` returns true for ANY instant/sorcery whose oracle_text contains "target", which would gate these on a non-empty stack (counterspell behavior). `getSpellPlan` must classify the new kinds BEFORE the counterspell fallback, and `canCastHandSpell` special-cases damage/pump/creature_effect/draw to cast at natural speed bypassing `getCanQuickCast`.
- **Trigger event detection is fully decoupled from targeting (migrations 076–082).** `fire_card_triggers(session, game_card, events[])` scans the card's effective script for `triggered_abilities` whose `event` matches and calls `enqueue_triggered_ability` — identically for every event. So adding a new event = add a DETECTOR that calls fire_card_triggers with new event names; targeting/target_controller work automatically. Detectors: `fire_zone_change_triggers` (game_cards zone change → enters_the_battlefield/dies/leaves_the_battlefield), `fire_turn_step_triggers` (game_turn_state step change → beginning_of_upkeep/draw_step/end_step; migration 082 replaced the upkeep-only `fire_upkeep_triggers`), `fire_attack_triggers` (game_combat_assignments insert → attacks), `fire_block_triggers` (game_combat_blockers insert → blocks), `fire_target_triggers` (game_stack_items insert for creature-target action_types → becomes_targeted; INSERT-only so it's loop-free and lands above the targeting item). Builder vocabulary `BUILDER_TRIGGER_EVENTS` (card-behavior-builder.ts) must list a new event's primary value or the form rejects it (parseTriggeredAbilities) and the LLM guide won't mention it; SQL accepts the aliases. Event field in Zod schema is just z.string() (no validation gate).
- **Targeted creature triggers + controller restriction (migrations 080–081):** triggered_ability stack items carry `target_required`/`target_card_id`/`target_controller` in payload. Controller picks the creature via `choose_triggered_ability_creature_target` RPC (client: `chooseTriggeredAbilityCreatureTarget`; UI: `TargetedTriggerPrompt` + `mustChooseTriggerTarget` blocks passing priority via `blockPassReason`). A trigger effect requires a chosen creature target ONLY when its type is targetable (deal_damage/destroy/bounce/tap/untap/add_counters) AND `target_type` is **creature-only** (`behavior_target_type_is_creature_only`) — "any target" damage triggers fall through to `apply_triggered_ability_effects`' each_opponent default instead of being dropped. `target_controller` (any|opponent|you; normalized from any/opponent/you/self/controller) enforced by SQL helpers `session_has_targetable_creature` + `creature_target_controller_ok`, used in enqueue (existence check), choose, resolve fizzle guard, and put_action_on_stack (spell casts). Client mirror: `creatureMatchesController` / `readTargetController` filter pickers; SpellPlan kinds carry `targetController`; put-on-stack action fns take a trailing optional `targetController` param. No-legal-target softlock avoided: enqueue won't enqueue if no legal creature; resolve guard only raises when a legal creature still exists, else fizzles.
- **Battlefield→graveyard chokepoint `put_in_graveyard(session, game_card)` (migration 084, Phase 0 of the rules-engine cleanup).** Single primitive for the dies/leaves-to-grave transition (returns bool; standard cleanup: zone→graveyard at owner's next position, controller→owner, is_tapped=false, damage_marked=0, dealt_deathtouch_damage=false, plus_one_counters=0). It does NOT rebuild continuous effects or fire triggers — the per-row zone-change trigger fires dies/leaves automatically; callers rebuild once after a batch. All three death sites route through it: `move_lethal_damaged_creatures_to_graveyard` (now snapshots the dying id set with array_agg, then loops put_in_graveyard — preserves "evaluate dying set once"), `resolve_top_of_stack` destroy_creature, `apply_targeted_triggered_ability_effects` destroy. This is the seam for the future finality counter / death-replacement layer (change one function) and for moving resolution to app code. Note: exile→exile and bounce→hand stay inline (different zones); counter_spell stack→graveyard is NOT a battlefield death and is untouched. Deliberate harmless unification: SBA deaths now also reset controller→owner (graveyard cards are owned, not controlled). **The decision (from the architecture discussion): keep authority + atomicity in Postgres; centralize in SQL (put_in_graveyard, future apply_effect / effective_characteristics); only build a pure-TS `reduce(state,action)` rules core at the replacement-effect/layer frontier (Noctis-class) — justified by shared client/server optimistic UI + deterministic replay, NOT a rewrite-now.**
- **Effect roadmap + exile/mill (migration 083, README "Effect roadmap" `<details>` block).** Adding a creature-targeting effect = mirror destroy everywhere it appears: the action_type CHECK constraint, `trigger_effect_requires_creature_target` list, `apply_targeted_triggered_ability_effects` branch, `put_action_on_stack` (not-in list + combined creature-target validation branch + payload case), `resolve_top_of_stack` branch, plus client `CREATURE_EFFECT_MAP` (key = spell action type e.g. `exile`→`exile_creature`), `TargetedCreatureActionType`, and schema (`z.enum(['destroy','exile','bounce','tap','untap'])` + KNOWN_V2_ACTION_TYPES). exile → owner's exile zone (fires leaves_the_battlefield, not dies). Adding an auto-resolved (untargeted) effect = edit ONLY `apply_triggered_ability_effects` + Zod member + KNOWN_V2_ACTION_TYPES + LLM prose; mill lives there (recipient controller default/each_opponent/each_player, mills top-of-library → graveyard). mill is trigger-only — no spell action yet (player-targeted mill needs a player picker). Effects authored via JSON/AI + Zod, NOT the guided form (BUILDER_EFFECT_TYPES), same as the targeted-creature effects — only add to BUILDER_EFFECT_TYPES if you also add form model/parse/serialize/UI.
- **Effective-script accessor `effective_script(session, game_card)` (migration 085, Phase 0).** Single STABLE SECURITY DEFINER function returning `coalesce(game_cards.copied_script, cards.script)` (null if the card isn't in the session). Replaces the inlined coalesce in the 3 *live* callers: `register_card_continuous_effects` (was 067), `activate_ability` (was 072), `fire_card_triggers` (was 077) — all other historical hits (044/048/049/050/055/063/064/067 register redefinitions) were superseded. Each caller reproduced verbatim with ONLY the script fetch swapped (activate_ability keeps its zone+ownership check by selecting `zone` alone then calling the accessor; register keeps reading `cards.keywords` separately since keywords are base-card data, not script). Behavior-preserving today; it is the one seam where DFC active-face → face-specific script selection will live. NOTE: `rebuild_scripted_continuous_effects` (063) uses a *different* shape — `card_behavior_has_continuous_effects(coalesce(copied_script,'{}'))` (presence prefilter, no fallback to cards.script) — left untouched, not part of this accessor.
- **Creature-effect primitive `apply_creature_effect(session, kind, target_card_id, params)` (migration 086, Phase 0).** Single SECURITY DEFINER function owning every targeted-creature mutation — kind ∈ {deal_damage, destroy, exile, bounce, tap, untap, add_counters, pump}; params jsonb carries `amount` (deal_damage/add_counters) or `power`/`toughness` (pump). No-ops if target null or off-battlefield (fizzle). destroy → put_in_graveyard; deal_damage re-runs move_lethal SBAs. Both former call sites now delegate: `apply_targeted_triggered_ability_effects` passes the effect `type` as the kind (non-creature effects still defer to `apply_triggered_ability_effects`); `resolve_top_of_stack` collapsed its 8 `*_creature` action branches into ONE `in (...)` branch that derives the kind via `regexp_replace(action_type,'_creature$','')` and hands the whole payload as params. This was the last logic duplication of the "mirror destroy in 2 places" tax. **Updated rule for adding a creature-targeting effect:** implement the mutation ONCE in apply_creature_effect, then only the declarations/validation remain (action_type CHECK constraint, `trigger_effect_requires_creature_target` list, `put_action_on_stack`, client `CREATURE_EFFECT_MAP`/`TargetedCreatureActionType`, Zod enum + KNOWN_V2_ACTION_TYPES) — no duplicated branch logic. Safe because `trigger_effect_requires_creature_target` only admits types apply_creature_effect handles, so a trigger effect can't hit its `else raise`. With this + effective_script (085) + put_in_graveyard (084), the two SQL Phase-0 centralizations from the Decision Log are complete; remaining Phase-0/1 work is the untargeted side already lives solely in apply_triggered_ability_effects, then the generic "pending decision" state machine.
- **Rules-engine test harness (`tests/`, the "test chamber").** Programmatic regression suite driving the real RPCs against **local Supabase** Postgres. Key facts: (1) uses a **direct `pg` connection, not supabase-js**, because `auth.uid()` resolves from `request.jwt.claims ->> 'sub'` and only a raw connection can set that GUC on the same connection before the RPC — `harness/db.ts` `asPlayer(client, playerId, fn)` does `set_config('request.jwt.claims', {sub,role}, true)` + `set local role authenticated`, then `reset role` so assertions read tables as postgres (RLS bypassed). (2) Each test runs in a transaction rolled back at the end (`withRolledBackTx`) → isolation without `db reset`. (3) **No `auth.users` seeding needed** — `game_session_players.player_id` has NO FK to auth.users (migration 009), so any random uuid is a valid player. (4) Setup reuses existing `dev_*` RPCs (`dev_spawn_card`, `dev_set_turn_state`, `dev_clear_summoning_sickness`). (5) `resolve_top_of_stack` can be called directly (doesn't require priority to have passed — that's `pass_priority`'s job), so it's a clean unit-resolve. Runner: `node --import tsx --test` (node:test, no vitest); Node's `--test` glob is unreliable on Windows so package.json points at the file explicitly. `Scenario` API in `harness/scenario.ts`: create/spawn/spawnCreature/setTurn/as(seat).putOnStack|activate/resolveStack + inspectors. **VERIFIED WORKING 2026-06-02** — proving trio (R1 destroy→put_in_graveyard, R8 activate→effective_script, R11 draw) passes. **Local schema = SQUASH, not migration replay** (see Decision Log + bug-104): `supabase/migrations/` holds only `00000000000000_baseline.sql` (full hosted `db dump --schema public`, reproduces prod incl. 079-086) + `00000000000001_local_test_relax_fks.sql` (LOCAL ONLY — drops the auth.users/profiles FK chain so throwaway player UUIDs work; never apply to hosted). The 88 incremental migrations were moved to `supabase/migrations_archive/` (Supabase ignores them; kept as history). `% Test` cards are schema-only-dump-excluded, so the harness seeds them from `tests/fixtures/test-cards.json` via `ensureTestCards` (committed once, idempotent). Gotchas burned in: turn **phases** are `beginning|main_1|combat|main_2|ending` (NOT the step names — `precombat_main` is a step; a main-phase instant window = phase `main_1` + step `precombat_main`); `asPlayer` cleanup must be try/catch-wrapped or a failed RPC's real error is masked by 25P02 on the aborted tx; FK chain is `game_cards.owner_id -> profiles.id -> auth.users.id`. `supabase/_hosted_schema.sql` is the gitignored scratch dump used to author the baseline.
- **Phase 1 pending-decision state machine (migrations 087-088).** 087 extracts `finalize_stack_resolution(session, stack_item)` — the shared resolve tail (mark resolved → rebuild_scripted_continuous_effects → reset priority → maybe_finish → return json); `resolve_top_of_stack` now ends with `return finalize_stack_resolution(...)`. 088 adds `game_pending_decisions` (id, session, deciding_player_id, source_stack_item_id, decision_type, prompt, options jsonb, min/max_choices, status pending|resolved|cancelled, result jsonb), `submit_decision(decision_id, result)` (the single entry point — validates deciding player + legal choice), `get_pending_decisions(session)`, `stack_item_has_pending_decision(item)`, and the **modal Tier-A** vertical slice: `cast_modal_spell(session, modes jsonb, choose, source)` puts a `modal_spell` stack item + a `choose_mode` decision; `resolve_top_of_stack` gained a `modal_spell` branch that refuses to resolve until the decision is `resolved`, then runs the chosen mode(s)' actions via `apply_triggered_ability_effects`. **Tier-A modes are UNTARGETED only for now** (targeted modes need target-selection, a later slice). Added `modal_spell` to the `game_stack_items_action_type_check` constraint. Result shape for choose_mode: `{"chosen":[idx,...]}` (or a single int). This is the seam for scry/distribute/choose-player (more decision_types) and Tier-B resolution-time decisions (park item → submit_decision applies outcome → finalize_stack_resolution).
- **Phase 1 Tier-B resolution-time decisions — scry (migration 089).** First true suspend/resume through `finalize_stack_resolution`. Contrast with Tier-A modal (088), where the decision is created on announcement and resolution merely refuses until it's chosen: in Tier-B the decision exists ONLY because resolution reached a point needing input. Flow: `cast_scry(session, amount, source)` pushes a `scry` stack item (no decision). `resolve_top_of_stack`'s `scry` branch looks at the top N of the caster's library, creates the `scry` decision (options = `[{game_card_id,name,library_position}]`), sets the stack item status to **`awaiting_decision`**, and **returns early `{awaiting_decision:true,...}` WITHOUT calling finalize** — so the `where status='pending'` resolve query never re-picks it (no re-entry guard needed). `submit_decision`'s `scry` branch (result `{top:[id...],bottom:[id...]}`, each ordered top-to-bottom) validates the submitted ids are exactly the revealed set (count match, no dups, all in options), renumbers the WHOLE library (chosen top first in order → un-revealed cards keeping order → chosen bottom in order, `row_number()`-1), then calls `finalize_stack_resolution(session, source_stack_item_id)` to resume. Required two constraint widenings: `'scry'` added to `game_stack_items_action_type_check` (full list = 088 + scry) and **`'awaiting_decision'`** added to `game_stack_items_status_check` (was pending|resolved|cancelled). Library convention: top = LOWEST zone_position (matches draw / dev_undo_last_draw). Empty library → finalize immediately (nothing to scry). Harness: `Scenario.castScry(amount)` + `libraryIds(seat)` (ordered top-to-bottom); test gotcha — in Tier-B the decision only appears AFTER `resolveStack()`, so call castScry → resolveStack → pendingDecision (SC3/4/5 failed first run by reading pendingDecision before resolving). Suite now 34/34. This validates the 087 seam end-to-end; the same park→submit→finalize shape generalizes to surveil/order_cards/distribute.
- **Scry authorable in the card behavior editor (no migration — TS only).** Added `scry` as a V2 spell action: `card-behavior-schema.ts` (`KNOWN_V2_ACTION_TYPES` + union member `{type:'scry', amount:number}` — added BOTH per the catch-all gotcha), `card-behavior-llm.ts` (spell-action list entry + a "Scry 2. (instant)" example so the AI generator authors it), and the cast path in `ControllerListV4.tsx`: `SpellPlan` gained `{kind:'scry',amount,timing}`, `getSpellPlan` detects a `scry` action (after `draw`), `canCastHandSpell` casts it at natural speed, the card sheet got an `onScry` prop wired to a new `scrySpell` dispatch action → `castScrySpell` wrapper in `actions.ts` → `cast_scry` RPC. Scry is **untargeted** (acts on the caster's own library) so it follows the `draw` plan shape, not a target picker. Mirrors the destroy/exile/mill precedent: spell effects are authored via **AI/JSON + Zod validation**, NOT the guided form (the form models only keywords/triggers/activated abilities — it has NO spell_effect widget for ANY spell). **CAVEAT — trigger-scry validates but does NOT execute yet:** the schema accepts `{type:'scry'}` inside a triggered ability, but `apply_triggered_ability_effects` applies trigger effects inline and can't park mid-resolution, so a trigger that scrys is a no-op. Making trigger-scry (and trigger-surveil) work needs generalizing the Tier-B park/resume to fire from within a (possibly multi-effect) trigger resolution — planned alongside surveil. **CAVEAT — no in-game decision UI yet:** casting a scry spell puts it on the stack and it parks on resolution (status awaiting_decision), but there is no client `<PendingDecisionPrompt>`/`usePendingDecisions`/`submitDecision` wrapper, so a human player currently has no way to submit the top/bottom ordering. The authoring + cast-to-stack path is complete; the resolution prompt UI is the remaining client-wiring piece for live play.
- **Surveil (migration 090) — Tier-B, mirrors scry exactly.** Look at top N of library, bin any number into the graveyard, rest back on top. `cast_surveil` + a shared `scry`/`surveil` park branch in resolve_top_of_stack (`action_type in ('scry','surveil')`, decision_type = the action_type, prompt `initcap(type)||' '||N`) + a `surveil` branch in submit_decision. submit_decision result: `{graveyard:[id...], top:[id...]}` — together must equal the revealed set (count/dup/subset validated). On resume: move graveyard ids to graveyard (`zone='graveyard'`, position `max+ord`, `is_tapped=false`, in submitted order), renumber remaining library (kept-top first then un-revealed), `finalize_stack_resolution`. Added `'surveil'` to the action_type CHECK. Authoring identical to scry: schema action `{type:'surveil', amount}`, LLM guide + "Surveil 1." example, `SpellPlan {kind:'surveil'}` + `getSpellPlan`/`canCastHandSpell`/`onSurveil`/`surveilSpell`/`castSurveilSpell`. Harness `castSurveil` + tests SV1-4. Same two caveats as scry (no trigger-execution, no in-game decision UI).
- **Targeted modal modes (migration 091) + apply_modal_spell extraction.** A chosen modal mode's actions may now target a creature (charm pattern: "Choose one — destroy target creature; or gain 3 life"). The target rides on the choose_mode submission: `submit_decision(choose_mode, {chosen:[idx], target_card_id})`. submit_decision's choose_mode branch detects whether any chosen mode has a creature-targeting action (`type in (deal_damage,destroy,exile,bounce,tap,untap,add_counters,pump)` AND target_type `"creature"` scalar or array-contains-creature) and, if so, REQUIRES `target_card_id` to be a battlefield creature in the session (else raises). **Extracted `apply_modal_spell(session, stack_item)`** — runs a resolved modal: per chosen mode, per action, a creature-target action → `apply_creature_effect(session, type, target, action)` (behavior action type == apply_creature_effect kind, so no mapping needed; action json carries amount/power/toughness as params), else untargeted via `apply_triggered_ability_effects`. resolve_top_of_stack's modal_spell branch is now one line (`perform apply_modal_spell(...)`) and dropped its v_decision/v_mode_index/v_mode locals — same "centralize the tail" move as finalize_stack_resolution, so future modal changes touch ONE function. Harness tests MDT1 (destroy chosen creature), MDT2 (untargeted mode of a targeted modal needs no target), MDT3 (targeted mode without target rejected). Suite 41/41. **NOT yet authorable:** modal needs a new script shape (`spell_effect.modes` or similar) + a mode/target cast UI — bigger client feature, deferred; cast_modal_spell/cast_scry/cast_surveil are reachable from the harness but only scry/surveil have the spell-action authoring + cast button.
- **Pending-decision client UI (migration 092 + TS, no engine change).** Closes the "no in-game decision UI" caveat for scry/surveil/modal. Data: `getPendingDecisions(supabase, session)` (data.ts, wraps `get_pending_decisions`); `submitDecision(supabase, decisionId, result)` action (actions.ts, wraps `submit_decision`); types `PendingDecision`/`ScryOption`/`ModalModeOption` (types.ts). `useControllerGameState` fetches `pendingDecisions` in its Promise.all + adds a realtime sub on `game_pending_decisions`; exposes the array. ControllerListV4 picks `myPendingDecision = pendingDecisions.find(deciding_player_id === playerId)`, threads it + an `onSubmitDecision` action through `MainArea` (alongside `onChooseTriggerTarget`), and renders `<PendingDecisionPrompt>` next to `TargetedTriggerPrompt`. The prompt branches on decision_type: `ChooseModeBody` (toggle modes respecting min/max; if a chosen mode has a creature-target action — `modalModeIsTargeted` via CREATURE_TARGET_ACTION_TYPES + targetTypeMatches — show a battlefield-creature picker; submits `{chosen, target_card_id?}`), `ScrySurveilBody` (per-card Top vs Bottom(scry)/Graveyard(surveil) toggle, submits `{top,bottom}` or `{graveyard,top}`; order within a bucket = revealed order — drag-reorder is a future refinement). **Realtime gotcha (migration 092):** a new table emits postgres_changes only if it's in the `supabase_realtime` publication — `game_pending_decisions` wasn't, so 092 adds it (guarded by pg_publication_tables since ADD TABLE errors if already a member). Without it the prompt still appeared via coattails (the stack-item change from cast/resolve triggers a full reload) but a decision-only change wouldn't push. **Confirmed working in the running app by the user (2026-06-02)** — the decision prompt renders and the scry/surveil/modal flow completes end-to-end (typecheck + lint clean; harness 44/44).
- **Scry/surveil from a triggered ability — resumable trigger resolution (migration 093).** Closes the "trigger-scry validates but doesn't execute" caveat. New `apply_trigger_effects(session, stack_item, start_index) returns uuid` walks a trigger's `effects` from start_index: a normal effect is applied immediately (reusing `apply_targeted_triggered_ability_effects` per-effect — it already dispatches creature-target vs untargeted); a scry/surveil effect creates a pending decision, stashes the NEXT index on the stack item (`payload.resume_index`), parks it (`status='awaiting_decision'`), and returns the decision id (null = ran to completion). `resolve_top_of_stack`'s triggered_ability branch keeps the target_required fizzle check, then calls apply_trigger_effects(…,0) and returns early `{awaiting_decision}` if parked. `submit_decision`'s scry & surveil tails, after the reorder/bin, look up the source stack item's action_type: if `triggered_ability`, RESUME via apply_trigger_effects(…, resume_index) (which may park again on a later scry) and finalize only when it returns null; a standalone `scry`/`surveil` spell item still just finalizes. Proven by tests TSC1 (ETB scry parks+resolves), TSC2 (ETB surveil bins), TSC3 (an effect AFTER the scry runs only on resume — fixture "Scry Then Gain Test" = [scry, gain_life], life unchanged until submit). The refactor (per-effect application) did not regress the existing trigger tests. Suite 44/44.
- **Scry/surveil added to the guided Form (builder + form UI).** Two homes, both now functional: (1) **Trigger effect** — added to `BUILDER_EFFECT_TYPES` (+ BuilderEffect union, defaultEffect, effectToJson, parseEffects); the form's EffectEditor already renders a generic amount input, so no per-type UI needed. Works via migration 093. (2) **New "Spell effect (instant/sorcery)" section** — the form had NO spell-effect section before. Added `BuilderSpellEffect`/`BUILDER_SPELL_EFFECT_TYPES` (none|scry|surveil), `BuilderForm.spellEffect`, buildScriptFromForm emits `spell_effect.actions:[{type,amount}]` (+ included in the empty-script check), parseScriptToForm adds `spell_effect` to knownKeys + `parseSpellEffect` (returns the model only for a single scry/surveil action; ANYTHING else — destroy/exile/damage/multi-action — returns null so the editor stays in JSON mode, preserving prior behavior). New "Spell effect" section in CardBehaviorForm.tsx (select + amount). **Gotcha:** `CardBehaviorEditor.tsx`'s `emptyBuilderForm()` literal also needed the new `spellEffect` field (tsc caught it). Round-trips verified: scry-spell ↔ form, surveil-trigger ↔ form, destroy-spell → null (JSON mode). The LLM guide already documents scry/surveil; the form vocab is independent.
- **Multi-action untargeted spell program + the scry/surveil graveyard fix (migration 094).** Two bugs the user hit authoring Opt ("Scry 1, then draw a card"): (1) `cast_scry`/`cast_surveil` never moved the spell card to the graveyard (unlike `put_action_on_stack`, which bins an instant/sorcery on cast — baseline ~L5354), so a scry instant stayed in hand; (2) the cast path dispatched only ONE effect, so scry+draw couldn't both happen. Fix: a non-permanent spell's `spell_effect.actions` is an effect PROGRAM resolved like a trigger's effects. New `cast_spell_effect(session, actions, source)` mirrors put_action_on_stack's timing rules + hand→graveyard move and pushes a `spell_effect` stack item carrying `{effects, controller_player_id}`. `resolve_top_of_stack` gained a `spell_effect` branch that runs `apply_trigger_effects(…,0)` (parks on scry/surveil); `submit_decision`'s scry/surveil resume now fires for source action_type `in ('triggered_ability','spell_effect')`. Added `'spell_effect'` to the action_type CHECK. **Client cast path changed:** `getSpellPlan` now returns `{kind:'spell_effect', actions}` for any spell whose actions contain scry/surveil and are all in `UNTARGETED_SPELL_ACTION_TYPES` (scry/surveil/draw/gain_life/lose_life/mill/create_token) — this SUPERSEDES the old single-action `scry`/`surveil` plan kinds (removed). Dispatch → `castSpellEffect` (actions.ts) → `cast_spell_effect`. So scry/surveil spells (incl. Opt) now go through the program path and hit the graveyard; the standalone `cast_scry`/`cast_surveil` RPCs remain only for the harness/direct use. **This RESOLVES the earlier "make scry/surveil the spell's only action" caveat for untargeted combos** — Opt and similar now work end-to-end (test SE1: card→graveyard on cast, scry parks, draw runs on resume, 45/45). A spell mixing scry/surveil with a TARGETED action still isn't routed here (rare; would need target selection).
- **Form Spell-effect section is now a multi-action LIST (Opt is clickable).** `BuilderForm.spellEffect` changed from `BuilderSpellEffect | null` to `BuilderSpellEffect[]` (ordered list; empty = none). `BuilderSpellEffectType` gained `'draw'` (now scry|surveil|draw); `BUILDER_SPELL_EFFECT_TYPES` dropped the 'none' sentinel; added `defaultSpellEffect(type)`. buildScriptFromForm maps the list → `spell_effect.actions`; `parseSpellEffect` now returns `BuilderSpellEffect[]|null` — accepts an actions array where EVERY action is a plain `{type∈scry/surveil/draw, amount}` (keys ⊆ {type,amount}), else null → JSON mode (so targeted destroy/exile, recipient draws, unknown fields still bail). CardBehaviorForm.tsx renders the section as a list editor (+ Add spell action / type select / amount / ✕), mirroring the trigger-effects pattern. `CardBehaviorEditor.tsx` `emptyBuilderForm()` now spreads `[...EMPTY_BUILDER_FORM.spellEffect]`. **getSpellPlan broadened:** routes to `spell_effect` when `allUntargeted && (hasScrySurveil || actions.length > 1)` — so a multi-action untargeted combo (e.g. Opt) works even via the form, while a single `draw` keeps its dedicated path. Round-trips verified (Opt ↔ form; destroy & recipient-draw → JSON mode). So Opt = [Scry 1, Draw 1] is now buildable by clicking in the form. No engine/migration change (094 already handles execution).
- **Validator catch-all gotcha (card-behavior-schema.ts):** `UnknownV2ActionSchema` accepts any action type NOT in `KNOWN_V2_ACTION_TYPES`. Adding a type name to that list WITHOUT also adding an explicit union member makes the catch-all reject it → validation fails. Always add both together (learned adding pump/destroy/bounce/tap/untap).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-05-27] **MTG combat — multiple blockers damage order:** When multiple creatures block a single attacker, it is the **ATTACKER** (not the defender) who assigns damage order among blockers. The defender's declare blockers phase only determines *which* creatures block *which* attacker. Do not show a "stel schade-volgorde in" or damage-ordering UI element on the defender's declare blockers screen.

- [2026-06-02] **Test harness: an expected RPC rejection aborts the whole test transaction.** Each harness test runs in one `withRolledBackTx`. A failing RPC (e.g. asserting `resolve_top_of_stack` raises) puts that tx into `25P02` (aborted), so ANY statement after the `assert.rejects` fails too. Keep an expected-rejection as the LAST action in a test, or split the guard assertion into its own test (what MD1 needed). A future `expectReject` helper using SAVEPOINT/ROLLBACK TO would let a test continue after an expected failure. (Also: `asPlayer`'s cleanup is already try/catch-wrapped so it doesn't mask the real error with 25P02.)

- [2026-06-04] **getSpellPlan branch ORDER matters — put multi-action/decision spells before single-action cases.** A scry+draw spell (Opt) silently only drew: `getSpellPlan` checked the single `draw` action before the `spell_effect` (scry/surveil/multi-action) case, so `actions.find(type==='draw')` matched first and dropped the scry → a `draw_cards` stack item, no decision. The engine/DB were 100% correct (a parked decision proved it); the bug was pure client classification. Fix: the `spell_effect` detection must come BEFORE the `draw` (and any other single-action) return. When adding a new spell kind, place broader/multi-action matchers ahead of narrow single-action ones. Also: when an in-game feature "does nothing," check whether the live CLIENT build is current AND whether the client is even producing the right stack `action_type` (query game_stack_items) before suspecting the SQL — the DB had everything right here. [[bug-155]]

- [2026-06-04] **A pending decision must freeze priority — a parked stack item is NOT 'pending'.** When scry/surveil/modal parks, the stack item goes status='awaiting_decision'. `pass_priority` counts only `status='pending'` items, so after a park it saw an empty stack and called `advance_step` → the round advanced with the decision still open, and players kept passing. Fix (migration 095): `pass_priority` raises if any `game_pending_decisions` row is pending for the session (guard at the top). Client mirrors it: `passBlockReason` ('Resolve your decision' for the decider / 'Waiting for a decision' for others / 'Choose trigger target') disables the Pass button. `submit_decision`→`finalize_stack_resolution` resets priority (active player, pass_count 0) so the cycle restarts cleanly. submit_decision does NOT depend on priority (only deciding_player = auth.uid()), so the decider can submit even while priority shows the opponent. Regression: SC6. [[bug-156]]

- **More shared-decision choice types: tutor / discard / may (migration 096).** The pending-decision system is the generic seam — adding a choice = (1) a park branch in `apply_trigger_effects` that builds `options` + inserts a `game_pending_decisions` row with a new `decision_type`, (2) a `submit_decision` branch that validates + applies + resumes, (3) a `<PendingDecisionPrompt>` body. 096 added: **`game_pending_decisions.params jsonb`** (per-decision config, e.g. tutor destination), **`resume_or_finalize(session, item)`** helper (the shared "continue the program from resume_index, or finalize" tail — now used by scry/surveil/search/discard/confirm; standalone scry/surveil items finalize since their action_type isn't triggered_ability/spell_effect). New effect types (work in triggers AND spell_effect programs via apply_trigger_effects): **`search_library`** (tutor: options=library cards filtered by `filter.type_line` ilike; pick 0..count; `submit`→move to `to` hand/battlefield/top + **shuffle** + rebuild; decision_type `search_library`), **`discard`** (options=controller hand; min=max=least(count,handSize); decision_type `choose_cards`, params.to=graveyard), **`may`** (decision_type `confirm`, options=[], min/max 0, params.effects=inner; `submit {confirmed}` → on true run inner via apply_targeted_triggered_ability_effects — **inner must be simple, no nested decision in v1**). Client: `CardPickBody` (multi-select up to max, for search_library+choose_cards, submits `{chosen:[ids]}`) and `ConfirmBody` (Yes/No → `{confirmed}`). getSpellPlan: added them to `UNTARGETED_SPELL_ACTION_TYPES` + a `DECISION_SPELL_ACTION_TYPES` list; routing now = `allUntargeted && (hasDecisionEffect || length>1)`. Schema: added to KNOWN_V2_ACTION_TYPES + explicit union members (may.effects kept loose `z.array(z.record)` to dodge self-referential zod typing). LLM guide updated. Authored via JSON/AI (not the guided form). Tests TUT1/TUT2/DIS1/MAY1/MAY2 (51/51). **choose_player is the remaining 4th choice the user wanted — deferred** because it threads a chosen player into DOWNSTREAM effects, which needs `apply_triggered_ability_effects` to honor a specific recipient player id (a baseline-function change), unlike these three which are self-contained.
- **choose_player (migration 097) — done; the 4th choice.** Pick a player at resolution, then apply inner (player-directed) effects to them. **Key trick that avoided touching the baseline applier:** instead of adding a recipient-by-id param, `submit_decision` runs the inner effects with the CHOSEN player as the `p_controller_id` context and `recipient` forced to `'controller'` on each effect — so lose_life/deal_damage/gain_life/draw/mill all land on the chosen player via the unchanged `apply_triggered_ability_effects`. Effect spec: `{type:'choose_player', filter?:'opponent'|'any', effects:[...]}`. apply_trigger_effects parks decision_type `choose_player` with options = players (filtered; `left join profiles` for username, null in harness), min/max 1, params.effects=inner. submit validates the player is offered, then rewrites inner effects with `recipient:'controller'` and calls `apply_triggered_ability_effects(session, chosenPlayer, source, rewritten)` → resume_or_finalize. Inner effects must be simple player-directed (no nested decision). Client `ChoosePlayerBody` (player buttons, username ?? 'Player'). Schema member + KNOWN_V2 + LLM + getSpellPlan lists updated. Tests CP1 (chosen opponent loses 3), CP2 (non-offered player rejected). **All four user-requested choices now live: tutor, discard, may, choose_player (53/53).**

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-06-05] **Phase-3 targeting reach, slices 4a+4b — DIVIDED damage (mig 115) + MULTI-target TRIGGERS (mig 116).** The last two targeting-reach items, completing the axis. **4a Divided damage (Forked Bolt):** new action type `divided_damage` carrying an `allocations` array of {target_card_id|target_player_id, amount}; 104/105 dispatch (1 builder + 1 handler + 1 registry row + CHECK +1). Builder validates each target (creature via creature_target_controller_ok, or a session player), amounts ≥1, DISTINCT targets, and sum == total (trusts the client total, like every existing damage builder). Handler loops: creature → apply_creature_effect('deal_damage') (runs lethal SBA), player → life loss. Authoring: deal_damage Zod gained `divided?: boolean`; getSpellPlan returns a `divided_damage` plan when `divided:true`; client has a +/- allocation UI (dmgAlloc Record keyed 'card:'/'player:', cast enabled when remaining==0). Tests DD1–DD5. **4b Multi-target triggers (mig 116):** a removal trigger effect carries `targets:N` (same field as the spell side). Threaded through the mig-114 trigger-target path: new `trigger_effect(s)_target_count`; reproduced `enqueue_triggered_ability` (stamps target_count), `handle_triggered_ability` (the "not chosen" guard now also checks target_card_ids), and the BIG `apply_trigger_effects` (mig 111, ~200 lines) — its targeted-effect else-branch now loops apply over each id in `target_card_ids` when present, else the single target_card_id path; **a non-targeted effect still goes through the single path once (apply_targeted_… routes it to the untargeted applier), so it is never double-applied** — the key correctness subtlety. New RPC `choose_triggered_ability_targets(session, stack_item, uuid[])` validates up-to-N distinct legal targets (creature OR permanent, reusing mig-113/114 validators) and stores target_card_ids. The single-target picker/path is untouched (back-compat). Client: TriggerTargetPrompt accumulates `picked` (toggle, capped at target_count) + Confirm when target_count>1; `needsMyTarget` now also clears when target_card_ids is set; new actions/wrapper chooseTriggeredAbilityTargets. Harness Scenario.chooseTriggerTargets. Fixture Twin Slayer Test (ETB destroy up to two opp creatures). Tests MTT1–MTT4. **No Zod change for 4b** (the removal `targets` field already existed from mig 112's spell work; triggers share the effect schema). **278/278, tsc/lint clean, all prior trigger tests green (regression for re-reproduced enqueue/handle/apply).** **TARGETING REACH IS NOW COMPLETE** — players (choose_player), non-creature permanents (spell mig 113 + trigger mig 114), multi-target (spell mig 112 + trigger mig 116), divided damage (mig 115). Only divided damage on the TRIGGER side is unbuilt (no demand noted).

- [2026-06-05] **Phase-3 targeting reach, slice 3 — NON-CREATURE PERMANENT targets for TRIGGERED abilities (migration 114).** The trigger analogue of mig 113's spell-side permanent_effect: "when this enters/dies, destroy/exile/bounce/tap/untap target artifact/enchantment/land/planeswalker/permanent." **Discovery first:** "player-target triggers" (the other roadmap item) is ALREADY covered by `choose_player` (parks a player decision in triggers + spells, applying lose_life/gain_life/draw/mill) — so nothing to build there; surfaced to the user and they picked trigger-side permanent. **The announcement-time trigger-target machinery is baked around a single CREATURE** (enqueue hardcodes target_type='creature'; the picker validates via creature_target_controller_ok; the fizzle guard checks session_has_targetable_creature; require-detection gates on behavior_target_type_is_creature_only). **Generalised that path to "a target whose type the effect allows", keyed off the effect's target_type, keeping the creature path byte-identical** (a creature-only target_type still resolves to 'creature' everywhere — verified PTT4 + all pre-existing trigger tests still green). **9 functions, 4 new + 5 reproduced:** new `behavior_target_type_is_permanent_only`, `trigger_effect_target_type` (returns '"creature"' for creature-only kinds, the real target_type for removal kinds with a permanent target_type, else null — this is the new single source of "does this trigger effect need a pick"), `trigger_effects_target_type`, `session_has_targetable_permanent`; reproduced `trigger_effects_target_controller` (gate generalised), `enqueue_triggered_ability` (drives target_required/target_type off trigger_effects_target_type; branches the has-target check creature-vs-permanent), `choose_triggered_ability_creature_target` (validates by payload target_type — name/signature kept since the client RPC depends on it), `handle_triggered_ability` (fizzle guard branched), `apply_targeted_triggered_ability_effects` (gate changed from `trigger_effect_requires_creature_target` to `trigger_effect_target_type IS NOT NULL` so a permanent-target removal dispatches to apply_creature_effect — which already works on any permanent — instead of the UNtargeted applier; that misrouting was the actual bug for permanent triggers before). **SCOPE: only destroy/exile/bounce/tap/untap gain non-creature targeting** (deal_damage/add_counters/grant_keyword/fight/gain_control stay creature-only — creature-semantic). MULTI-target triggers (an array of targets) remain a separate follow-up. **No new action_type → no CHECK-constraint change** (triggers ride the existing 'triggered_ability' type). **No Zod change** (trigger effects share the spell effect schema, which already allows non-creature target_type). LLM: broadened the trigger + spell non-creature notes. **Client:** `TriggerTargetPrompt` now filters board cards by the payload target_type (isCreatureOnlyTargetType ? creatures : cardMatchesTargetType) — reused the mig-113 client helpers; empty-state copy generalised to "No legal targets". Fixture Artifact Smasher Test (ETB destroy target artifact). Tests PTT1 (destroy artifact), PTT2 (wrong-type rejects), PTT3 (no legal target → not enqueued, no softlock), PTT4 (creature path regression). 269/269, tsc/lint clean. **Targeting reach now: only DIVIDED damage + MULTI-target triggers remain; player-target triggers were already done.**

- [2026-06-05] **Phase-3 targeting reach, slice 2 — NON-CREATURE PERMANENT targets (migration 113).** destroy/exile/bounce/tap/untap a target artifact/enchantment/land/planeswalker/"permanent" (Disenchant, Naturalize, Vindicate, Beast Within). **Key realisation: the engine APPLY path needed ZERO change** — apply_creature_effect's removal kinds already operate on ANY battlefield card (they never check type_line='creature'; only deal_damage/pump/add_counters/fight/gain_control/grant_keyword are creature-semantic). The creature-only gates were purely the VALIDATOR (creature_target_controller_ok hardcodes '%creature%') and the client picker. **Design = ONE new single-target action type `permanent_effect`** (parallel to mig 112's multi_creature_effect), via the 104/105 dispatch (1 builder + 1 handler + 1 registry row); the creature-only action types (destroy_creature…) are UNTOUCHED. **Two new validators:** `card_type_line_matches_target(type_line, target_type jsonb)` — 'permanent'/'any' match anything on the battlefield, a concrete type matches by type_line substring, array matches if ANY element matches; `permanent_target_controller_ok(...)` — the non-creature analogue of creature_target_controller_ok (battlefield + type match + controller restriction). Builder defaults target_type to '"permanent"' when omitted. Handler just calls apply_creature_effect(kind, id). **Authoring needed NO Zod change** — the removal effects' target_type already accepted artifact/enchantment/permanent/array (BehaviorTargetTypeSchema); it simply had no cast path before. LLM caveat updated (was "creature-only, omit non-creature"; now documents target_type artifact/enchantment/land/planeswalker/permanent for destroy/exile/bounce/tap/untap; add_counters/pump/fight/grant_keyword/gain_control stay creature-only). **Client:** `castPermanentEffect` wrapper; getSpellPlan routes a removal effect whose target_type is NOT creature-only (new `isCreatureOnlyTargetType` helper) to a `permanent_effect` plan; a permanent picker filtered by `cardMatchesTargetType` (client mirror of the SQL matcher) + controller; folded into the target plumbing as a parallel `requiresPermanentTarget`/`hasPermanentTargets`/`targetablePermanents` (the existing flow was creature-centric). Same action_type CHECK gotcha as bug-235 (handled up front this time, bug-242). Tests PE1 (destroy artifact), PE2 (exile enchantment), PE3 (type mismatch rejects), PE4 (target_type 'permanent' hits a creature too), PE5 (controller restriction), PE6 (array target_type) — fixtures Green Mana Vessel Test (Artifact) / Exploration Test (Enchantment). 265/265, tsc/lint clean. **NOTE: apply_creature_effect is now a misnomer (applies to any permanent for removal kinds) — not renamed (huge churn).** **Remaining targeting reach: player-target triggers, divided damage, trigger-side multi/permanent.**

- [2026-06-05] **Phase-3 targeting reach, slice 1 — general MULTI-TARGET removal (migration 112).** First effect with an arbitrary-length target set. Scoped via AskUserQuestion to: removal family (destroy/exile/bounce/tap/untap), **up to N** creatures, **full effect to each** (NOT divided — divided damage like Forked Bolt is a much larger separate shape and was explicitly deferred), **spell side first** (triggers a follow-up). **Design = ONE new castable action type `multi_creature_effect`** carrying `kind` + a `target_card_ids` ARRAY, plugged into the mig 104/105 data-driven dispatch with **ZERO reproduction of put_action_on_stack / resolve_top_of_stack** — just 1 builder + 1 handler + 1 registry row. The existing single-target action types (destroy_creature etc.) are **UNTOUCHED**, so their behaviour/tests are unaffected (no regression surface). **Builder `build_stack_payload_multi_creature_simple`** validates `kind` in the removal set + that EVERY id is a legal creature via the SAME `creature_target_controller_ok` gate the single-target builders use (targets locked at cast, MTG-correct); rejects empty set. **Handler `handle_multi_creature_effect`** loops `apply_creature_effect(kind, id)` — which already no-ops a target that left the battlefield, so a partially-illegal set still resolves for the legal ones (rule 608.2b/c), verified by MT4. **GOTCHA (bug-235):** a new castable action_type needs adding to the `game_stack_items_action_type_check` CHECK (last redefined mig 107) — registering the handler/builder is not enough; the INSERT fails otherwise. mig 112 reproduces that allow-list + 1. **Authoring:** removal effects gained optional `targets?: number` (Zod + LLM); `targets > 1` → multi. NOT a new form field (the guided form's single creature picker covers single-target; multi is JSON/AI-authorable for now). **Client:** `castMultiCreatureEffect` wrapper; `getSpellPlan` detects `targets > 1` on a removal effect → a new `multi_creature` plan; a multi-select picker (toggle creatures, capped at count, confirm) wired through `onMultiCreatureEffect`, modelled on the creature_effect/fight pickers. **Test gotcha (bug-236):** the harness runs one tx per test, so an expected-raise poisons it — a rejection test (MT5/MT6) must not run post-assertion queries. Tests MT1 (destroy two), MT2 (tap two), MT3 (bounce two), MT4 (partial fizzle), MT5 (illegal target rejects cast), MT6 (empty rejects) — cast via put_action_on_stack directly, **zero new fixtures**. 259/259, tsc/lint clean. **Remaining targeting reach: non-creature permanents + player-target triggers (the other two slices), and divided damage.**

- [2026-06-05] **Tier-3 `search_library` variants (migration 111) — graveyard dest, battlefield-tapped, name filter, reveal metadata.** Four backward-compatible additions to the existing tutor (every new field optional; defaults preserve current behaviour), all confined to the two `search_library` branches of the reproduced `apply_trigger_effects` + `submit_decision` (mig 110 base): **(1) `to:'graveyard'`** (Entomb/Buried Alive) — a 4th destination branch in the submit apply loop, mirroring the choose_cards generic `zone=v_dest` move. **(2) `tapped:true`** (Rampant Growth/fetchlands) — carried in params; the battlefield branch reads `is_tapped = coalesce((params->>'tapped')::boolean,false)` (was hardcoded false). **(3) `filter.name`** — the options query gains `and (v_name is null or c.name ilike '%'||v_name||'%')` alongside the existing type_line match; prompt appends "named X". **(4) `reveal:true`** — NO real sink exists in this engine ("reveal" everywhere else just means "the option set shown for a decision", never recorded state; there's `is_face_down` but no `is_revealed`, no game-log table). Implemented as the honest minimal sink: stamp the found ids into `result.revealed` via `update ... returning * into v_decision` (so the row returned to the caller carries it) — auditable METADATA only, no opponent-facing UI broadcast. **Authoring split (the key call):** `graveyard` went into the guided form (clean enum addition to SEARCH_DESTINATIONS — no breakage); **`tapped`/`reveal`/`filter.name` are JSON/AI-only** (Zod + LLM, NOT the registry), matching the gain_control untap/haste precedent. **Why name stayed out of the form (bug-230):** adding a `name` sub-field to the registry filter OBJECT broke 5 builder characterization tests — `parseFields` rejects a filter carrying only type_line (non-optional sub-field absent → JSON mode), and `fieldToJson` emits EVERY object sub-field with no empty-drop, so `name:''` would leak into every serialized tutor. The object-field machinery has no "optional sub-field that drops when empty" support; rather than deepen it, name is JSON/AI-authorable like the other extras. Zod: `to` enum += 'graveyard', added `tapped`/`reveal` booleans + `filter.name`. Tests SLG1 (graveyard), SLT1 (tapped)/SLT2 (default untapped guard), SLN1 (name filter narrows options), SLR1 (reveal records ids)/SLR2 (no reveal → no key) — all via `castSpellEffect`, **zero new fixtures**. 253/253, tsc/lint clean. **Remaining Tier-3 frontier: targeting reach (player/permanent/multi-target).**

- [2026-06-05] **Multi-opponent edict `sacrifice who:'each_opponent'` (migration 110) — sequential decision CHAIN, the deferred mig-108 follow-up.** "Each opponent sacrifices a creature" (Plaguecrafter): every opponent chooses their OWN creature, so it's N sequential decisions that must all finish before the program resumes. **Mechanism — NO new decision_type (reuses 'sacrifice'), no stack changes:** the parked decision's `params` now carries the remaining-opponent QUEUE (`edict_queue`, jsonb array of player-id strings) plus `count`/`filter`. When a sacrifice resolves, submit_decision parks the NEXT opponent from the queue instead of resuming; only when the queue empties does it `resume_or_finalize`. **resume_index is set ONCE** (in apply_trigger_effects, past the sacrifice effect) and untouched across the whole chain — the stack item just stays `awaiting_decision` between opponents. **Shared helper `park_edict_sacrifice(session, stack_item, count, filter, queue)`** does build-options-and-park for the first queue candidate that controls an eligible permanent (skips empty boards, tries next), stamping the leftover queue into params; returns the decision id or null if NObody can sac. **Both the initial park (apply_trigger_effects) and every continuation (submit_decision) call it** — so `who:'you'` (queue=[controller]) and `who:'opponent'` (queue=[first opponent]) now route through it too with an empty leftover queue (one decision → resume), keeping **SAC1/2/3 behaviour-identical** (verified: 247/247, the prior sacrifice tests still pass after the refactor). who→queue in apply_trigger_effects: each_opponent = all opponents `order by seat_number`; opponent = first opponent only; you = [controller]. Queue elements are `to_jsonb(player_id::text)`; remaining computed via `jsonb_array_elements ... with ordinality where ord > v_idx+1`. **Zero client change** — each opponent's decision is just another `sacrifice` CardPickBody assigned to its `deciding_player_id`; submit_decision has no priority check so the assigned opponent can submit any time. **Harness gained 3-player support:** `Scenario.create(client, numPlayers: 2|3)` (Seat now 'A'|'B'|'C'; C joins only when numPlayers=3, its UUID always exists for typing). Tests ME1 (B then C chain, controller's own creature untouched), ME2 (empty last opponent skipped → resolves), ME3 (empty FIRST opponent skipped on the initial park). Authoring: Zod `who` enum += 'each_opponent', LLM prose (both sacrifice notes). tsc/lint clean. **All sacrifice variants now shipped.**

- [2026-06-05] **Tier-3 X spells (migration 109) — variable amount paid as real {X} generic mana, BOTH cast paths.** Scoped via two AskUserQuestion forks: user chose **pay real {X} mana** (not free-pick) AND **both shapes** (targeted Fireball + untargeted Mind Spring). **Core design = resolve X server-side, tied to mana paid:** the card script writes the literal string `amount: "X"` (Zod `AmountSchema = z.union([z.number(), z.literal('X')])`, applied to deal_damage/draw/gain_life/lose_life/mill/add_counters); the client/caller sends a chosen `x_value`; the SERVER charges `x_value` generic mana THEN substitutes it into the effect amount. A client can't pay 1 and deal 999 — the damage is whatever mana was paid. **Three seams: (1) `pay_mana_cost` gains `p_x_value` (default 0) and parses `{X}`** — the old regex `([0-9]+|[WUBRGC])` silently DROPPED X; new code counts X symbols (`length - length(replace(...,'X',''))`) and adds `x_count * x_value` to generic cost. **Drop+recreate (not a new overload)** so all existing 4-arg callers resolve to the single 5-arg-with-default function (same trick used for cast_spell_effect). **(2) Targeted path (`put_action_on_stack`, mig 105 skeleton reproduced):** reads `(payload->>'x_value')::int`, forwards it to pay_mana_cost; the 4 amount-builders (deal_damage_player/creature, add_counters_creature, draw_cards) resolve amount via new helper `resolve_effect_amount(raw, x)` = `case raw='X' then x else raw::int`. **Signature UNCHANGED — X rides in the jsonb payload** (no client RPC-arg churn for the targeted path). **(3) Untargeted path (`cast_spell_effect`, mig 094 reproduced):** gains `p_x_value`, substitutes `"X"`→x_value in the top-level effects list (jsonb_agg with `|| jsonb_build_object` merge, so apply_trigger_effects never sees "X"), AND — **BEHAVIOR CHANGE** — now PAYS the source card's mana cost. The untargeted program path previously paid NOTHING (only put_action_on_stack paid). Guarded by source-from-hand + non-empty mana_cost, so all free-cast fixtures (no mana_cost) are unaffected; treated as in-scope for "pay {X}" (you can't charge the X part without charging the cost). **Minimal client decision deferred to Slice 2:** no mana-color picker needed (engine auto-distributes generic when generic_payment null) — the only new client input is a "choose X" number prompt. **Harness needed mana for the first time:** added `Scenario.setMana(seat, pool)` (direct UPDATE) + `manaOf` + `castSpellEffect` xValue passthrough; seed.ts + TestCard type gained `mana_cost` (was never loaded — fixtures are all free); fixtures Fireball Test {X}{R} / Mind Spring Test {X}{U}. Tests X1 (Fireball→creature, damage_marked=X, mana charged), X2 (→player, life & mana), X3 (insufficient generic rejects), X4 (Mind Spring draws X, charged), X5 (untargeted path also gates on {X}). **244/244, tsc/lint clean.** **Slice 2 (client) SHIPPED:** `getSpellPlan`'s damage/draw/add_counters/spell_effect branches now accept `amount==='X'` (widened the casts to `number | 'X'`) and set `xRequired` (amount 0 placeholder); the 5 cast handlers (dealDamageToPlayer/Creature, addCountersCreature, drawCards, spellEffect) call a new module-level `promptForXValue()` (window.prompt → positive int, abort on cancel) when `xRequired` and pass the result as `xValue`; the 5 actions.ts wrappers gained a trailing `xValue` param that sends `amount:'X' + x_value:N` (server resolves — never trusts a client number). **No mana-colour picker** (engine auto-distributes generic). Note: in the client a single-action `draw amount:"X"` (Mind Spring) routes to the `draw` plan → `put_action_on_stack('draw_cards')`, NOT cast_spell_effect — so X is exercised through BOTH `put_action_on_stack` (builders + pay_mana_cost) and `cast_spell_effect`; both resolve it. **X spells fully complete end-to-end.** Multi-opponent edict still deferred (user: "xspells first, defer multi after").

- [2026-06-05] **Tier-2 `sacrifice` + `return_from_graveyard` (migration 108) — both decision-machine effects, BOTH directions/destinations.** The last two Tier-2 backlog items, shipped together because both are pure `apply_trigger_effects` park/resume effects (NO stack action_type, NO put/resolve reproduction — they ride inside existing `triggered_ability`/`spell_effect` items). Pattern = exactly the 096/097 recipe: reproduce `apply_trigger_effects` + 2 park branches and `submit_decision` + 2 apply branches; **`decision_type` is free text (no CHECK)** so nothing to widen. **(1) sacrifice** `{who?:'you'|'opponent', count?, filter?}` — the SACRIFICING player owns the choice, so the parked decision's `deciding_player_id` = that player: `who:'you'`→controller, `who:'opponent'`→the single other seat (`player_id is distinct from v_controller order by seat_number limit 1`; 2-player edict — multi-opponent "each opponent sacs" = sequential decisions, deferred). Options = creatures the decider CONTROLS (`controller_player_id = decider`, so a stolen creature is sac-able); apply loops **`put_in_graveyard`** (the dies chokepoint → fires dies triggers + leave cleanup + controller→owner), then rebuild. Mandatory up to N (min=max=least(count,available), like discard). **(2) return_from_graveyard** `{to?:'hand'|'battlefield', count?, filter?}` — mirrors `search_library` but source = controller's OWN graveyard, NO shuffle, `to` hand (Raise Dead) or battlefield (Reanimate — controller=owner, untapped, entered_battlefield set → fires ETB). min 0/max count (may decline, matching tutor). **Both default `filter` to creatures** (`case when filter then type_line ilike filter else ilike '%creature%'`), overridable for other permanents/cards. Both fold into submit_decision's existing `{chosen}` validation `in(...)` list and the client's `CardPickBody` (same `{chosen:[ids]}` shape as tutor/discard — **zero new decision UI**) + `UNTARGETED_SPELL_ACTION_TYPES`/`DECISION_SPELL_ACTION_TYPES`. Authoring JSON/AI only (Zod members + KNOWN_V2 — both per the catch-all gotcha; LLM prose + 2 examples); NOT the guided form, consistent with every other decision effect. Tests SAC1 (you), SAC2 (edict → deciding_player=B), SAC3 (no eligible → no decision, resolves clean, no lock), RG1 (hand), RG2 (reanimate untapped/owner-controlled), RG3 (default creature filter skips a graveyard instant). **239/239** (also folded the prior session's gain-control.test.ts into `npm test` — it had been run directly but never added to the runner), tsc/lint clean. **All Tier-2 effect-vocabulary items are now shipped** (grant_keyword, fight, gain_control, sacrifice, return_from_graveyard).

- [2026-06-04] **Tier-2 `grant_keyword` — target creature gains a keyword until end of turn (migration 099).** Modeled exactly on `create_pt_pump`'s until-EOT row: a new `grant_keyword` kind in `apply_creature_effect` inserts a `game_continuous_effects` row (`effect_type` = the keyword, `affected_card_id`=`source_card_id`=target, `source_zone_required='battlefield'`, `expires_at_phase='ending'`/`expires_at_step='cleanup'`). The existing `card_has_<keyword>` accessors read it and the existing `expire_continuous_effects_for_step` sweep removes it — **zero new lifecycle code, and no constraint change** (every grantable keyword is already a member of the `game_continuous_effects` effect_type CHECK). Allowed keywords: flying/reach/trample/vigilance/haste/first_strike/double_strike/deathtouch/indestructible (guarded with a raise in apply_creature_effect). **Scope decision: TRIGGER-ONLY** (registry `contexts:['trigger']`, NOT in BuilderSpellEffect, NOT a spell action). The instant/combat-trick spell path was deferred because it would require reproducing the ~320-line `put_action_on_stack` verbatim (a new `grant_keyword_creature` action_type + payload case carrying `keyword`) PLUS untestable client picker wiring (`CREATURE_EFFECT_MAP`/`getSpellPlan`/a keyword-choice UI) — high transcription risk for low incremental value. Mirrors how `mill` shipped trigger-first. **Key enabling change:** `apply_targeted_triggered_ability_effects` now passes the WHOLE effect jsonb to `apply_creature_effect` (was `jsonb_build_object('amount',…)`), so non-amount params like `keyword` survive — behavior-preserving for existing targeted kinds (they only read `amount`). Reachable as a targeted trigger because `grant_keyword` was added to `trigger_effect_requires_creature_target` (creature-only target_type → routes through the existing `choose_triggered_ability_creature_target` picker). Verified end-to-end in the harness (GK1 grant applies + lapses at ending/cleanup via new `Scenario.expireEffects`; GK2 controller restriction). Authoring: registry entry (keyword enum + composite creatureTargetField), Zod union + KNOWN_V2, BuilderEffect union, LLM prose. 203/203. **Follow-up if combat tricks (instants) are wanted: the grant_keyword_creature spell path.**

- [2026-06-04] **Tier-2 `grant_keyword` SPELL/combat-trick path (migration 100) — the deferred follow-up, now shipped.** Adds `grant_keyword_creature` as a creature-targeting stack action alongside destroy/exile/etc. **Key realization that collapsed the risk:** the keyword is FIXED by the card's script (the author chose "gains flying"), so casting needs only a creature target — the same picker as destroy_creature; **NO keyword-picker UI** (the prior session over-estimated this). Went through `put_action_on_stack` (NOT a new cast fn like cast_spell_effect) specifically because put_action_on_stack calls `pay_mana_cost` — combat tricks must cost mana, and cast_spell_effect skips payment. Cost: reproduce TWO functions verbatim-with-surgical-inserts — (1) `put_action_on_stack` (baseline, ~320 lines): `grant_keyword_creature` added to the supported-action whitelist + the shared creature-target validation `in(...)` branch (+ a keyword-allowlist raise) + a payload `case` branch carrying `keyword`; declared `v_keyword text`. (2) `resolve_top_of_stack` (from mig 094): added to the collapsed creature `in(...)` branch — `regexp_replace('grant_keyword_creature','_creature$','')='grant_keyword'` dispatches to apply_creature_effect's existing kind, payload already carries keyword → ZERO new resolution/lifecycle code, expiry still via the cleanup sweep. Plus the `game_stack_items` action_type CHECK (from mig 094) gained the value. Client: `putGrantKeywordCreatureOnStack` wrapper (mirrors putAddCountersCreatureOnStack but carries `keyword`), `TargetedCreatureActionType` += the value, and `getSpellPlan` now detects `grant_keyword` → a `creature_effect` SpellPlan carrying optional `keyword`; the `creatureEffect` dispatch branches on `plan.keyword`. Authoring now BOTH contexts: registry `contexts:['trigger','spell']`, added to `BuilderSpellEffect`, LLM prose updated ("valid as triggered ability or instant/sorcery combat trick"). Tests: GK3 (cast→grant→lapses at cleanup), GK4 (bad keyword rejected), GK5 (target_controller honored), + unit spell round-trip. **208/208, tsc clean, lint clean (2 pre-existing img warnings).** This is a worked example of the "add a creature-targeting SPELL effect" recipe (vs the trigger recipe): the two reproduced functions are the whole tax.

- [2026-06-04] **Tier-2 `fight` (migration 101) — the first MULTI-target effect.** "Creature you control fights target creature" — each deals damage equal to its power to the other, simultaneously (CR 701.12), both can die. **Mechanic = pure reuse, zero new damage/death code:** `apply_fight(session, fighter, fought)` reads BOTH `card_effective_power` (printed + counters + pumps) UP FRONT (so a creature that dies still deals its hit — fight is simultaneous), then calls `apply_creature_effect('deal_damage', …)` twice (which marks damage + re-runs the lethal SBA sweep). Fizzles (returns, no damage) if EITHER creature has left the battlefield at resolution. **Cast-path decision: a dedicated `cast_fight` RPC, NOT put_action_on_stack.** Rationale: fight's two-target shape (asymmetric controller roles — fighter='you', fought='any') doesn't fit put_action_on_stack's single-target payload, AND a third verbatim reproduction of the ~320-line put_action_on_stack was avoidable. cast_fight is ~110 focused NEW lines modeled on `cast_spell_effect`'s auth/session/turn/priority/timing/source-move skeleton — but ADDS `pay_mana_cost` (cast_spell_effect omits it; fight spells should cost mana). Inserts a `fight_creatures` stack item with payload `{target_card_id: fighter, target_card_id_2: fought}`. `resolve_top_of_stack` reproduced (from mig 100) with a `fight_creatures` branch → apply_fight. Constraint (`game_stack_items_action_type_check`) += `fight_creatures`. Harness: `Scenario.castFight(fighter, fought, source?)` + fight.test.ts F1 (4/4 vs 1/1: small dies, big survives w/ 1 dmg) / F2 (2/2 vs 2/2 mutual kill) / F3 (rejects a fighter you don't control). 211/211, tsc/lint clean. **SCOPE = engine + spell path ONLY (deliberate).** Deferred follow-ups, documented in roadmap: (1) in-app **two-creature picker UI** (ControllerListV4 getSpellPlan + a new two-target picker — fight is the first effect needing two pickers, a real UI lift); (2) **form/Zod authoring** (no Zod member or BuilderSpellEffect added yet — adding them without the client picker would be speculative half-wiring); (3) **trigger-path fight** ("when this enters, it fights target creature" — the source is implicit fighter + one picked target, feasible with the existing single-target trigger picker but a separate slice); (4) generic-mana cost splitting in cast_fight (passes null = colored/auto only); (5) deathtouch interaction (apply_creature_effect deal_damage ignores the source's deathtouch). **Pattern note:** dedicated cast_* RPCs (cast_modal_spell/cast_scry/cast_surveil/cast_spell_effect/cast_fight) are the established home for spell shapes that don't fit put_action_on_stack's single-target/single-payload model.

- [2026-06-04] **`fight` FINISHED end-to-end (same session) — authoring + client two-step picker.** Completed the deferred slice from the engine-only increment above. **(1) Amended mig 101's `cast_fight`** (it was untracked/never-deployed, so editing was safe — verified via `git status` showing `??`) to add `p_fought_controller text default 'any'`, validated server-side via `creature_target_controller_ok` — so "you don't control" (Prey Upon) is enforced, not just client-guided. New signature `cast_fight(uuid,uuid,uuid,uuid,text)`. **(2) Authoring shape decision:** the `fight` action's `target_type`/`target_controller` describe the **FOUGHT** creature; the fighter is **implicit** ("a creature you control"). This let me REUSE the existing composite `creatureTargetField` (the `target` FieldDescriptor kind) verbatim in the registry — `{ type:'fight', contexts:['spell'], fields:[creatureTargetField] }` — so effectToJson/effectFromJson/parseFields needed ZERO changes (fight rides the same target_type/target_controller expansion as destroy/exile). Zod: a `fight` union member (target_type/target_controller/target_ref optional) + `'fight'` in KNOWN_V2. Builder: `{ type:'fight'; target:string }` added to **BuilderSpellEffect only** (spell-only; NOT BuilderEffect). **(3) Client two-step picker** (ControllerListV4): `getSpellPlan` detects `fight` → `{ kind:'fight', foughtController }`; a NEW `fightFighterId` state drives a two-phase picker (phase 1 = creatures you control = fighters; phase 2 = creatures matching foughtController, excluding the chosen fighter = fought); dispatch `actions.fight(cardId, fighter, fought)` → `castFight` wrapper. `hasFightTargets = fightFighters.some(f => fightFoughtFor(f.id).length>0)` gates the cast button. **Type gotcha:** `defaultEffect('fight')` is a TS error — `defaultEffect` is typed to `BuilderEffectType` (trigger union); a spell-only effect must use `defaultSpellEffect('fight')`. **Test gotcha for from-hand casts:** harness fixtures carry NO `mana_cost` (seed.ts inserts name/type_line/oracle_text/power_toughness/script only), so a from-hand spell casts FREE — no `dev_add_mana` needed (F5 casts `Prey Upon Test` from hand and asserts graveyard move). Tests: fight.test.ts F4 (fought_controller='opponent' rejects your own creature) + F5 (Prey Upon from hand) + unit classify/round-trip/default pins. Fixture `Prey Upon Test` (Sorcery). 217/217, tsc/lint clean. Still deferred: trigger-path fight, generic-mana split, deathtouch.

- [2026-06-04] **`fight` TRIGGER path shipped (migration 102) — the last deferred fight slice.** "When this enters, it fights target creature you don't control": the SOURCE creature (the one with the ability) is the fighter; the player picks the FOUGHT creature through the **existing** trigger target picker — **zero new client UI.** This worked because the trigger machinery already threads `source_card_id` + the picked `target_card_id` into `apply_targeted_triggered_ability_effects`. Two seams, both already built for grant_keyword (mig 099): **(1)** `trigger_effect_requires_creature_target` learns `'fight'` → the enqueue (`enqueue_triggered_ability`/baseline L3555) auto-marks the trigger `target_required` with the effect's `target_controller` (via `trigger_effects_require_creature_target` / `trigger_effects_target_controller`), and `session_has_targetable_creature` gates/fizzles it. **(2)** `apply_targeted_triggered_ability_effects` dispatches a `fight` effect to `apply_fight(session, SOURCE, picked target)` instead of `apply_creature_effect` (which has no `fight` kind). **Also added a self-fight guard to `apply_fight`** (`return` if fighter=fought): the trigger picker constrains the fought creature by controller but does NOT exclude the source, so without the guard a `target_controller:'any'` fight trigger could pick its own source and double-hit it; `cast_fight` already raised on self-fight before reaching apply_fight, so the guard is purely additive. Authoring: registry contexts `['spell']`→`['trigger','spell']`; `BuilderEffect` (trigger union) += `{type:'fight';target:string}` (it was only in BuilderSpellEffect); **no Zod change** — `CardBehaviorActionSchema` is shared by spell_effect.actions AND triggered_abilities[].effects, so `fight` already validated in triggers. LLM prose updated (fight listed among targeted creature triggers; "SOURCE is the fighter"). Fixture `Pit Brawler Test` (4/4 ETB fight, target_controller opponent). Tests: F6 (ETB source fights chosen creature, lethal + damage_marked), F7 (controller restriction rejects your own creature), F8 (no-legal-target fizzle/no-softlock) + unit `trigger fight` round-trip + `defaultEffect('fight')` pin (now valid since fight is in BuilderEffect). **222/222, tsc/lint clean.** Fight is now COMPLETE: engine + spell cast + spell authoring + two-step client picker + trigger path + trigger authoring. Remaining fight follow-ups are only generic-mana split + deathtouch. **Recipe contrast captured:** a creature-targeting effect that already routes through `apply_creature_effect` (grant_keyword) needs only the two trigger seams; `fight` additionally needs the dispatch branch because its mechanic (`apply_fight`) is NOT an apply_creature_effect kind — the source-as-implicit-first-target is the new shape.

- [2026-06-04] **`fight` tail finished (migration 103) — deathtouch + generic-mana parity. fight is now COMPLETE.** **(1) Deathtouch.** `move_lethal_damaged_creatures_to_graveyard` already destroys a creature with `dealt_deathtouch_damage=true` AND any marked damage (baseline L4747-4757); combat sets that flag via `card_has_deathtouch`. The gap: `apply_creature_effect`'s `deal_damage` only marked `damage_marked`, never the deathtouch flag — so fight (and any non-combat damage) ignored the dealer's deathtouch. Fix: deal_damage now honours an optional `'deathtouch'` boolean param (`dealt_deathtouch_damage = dealt_deathtouch_damage or coalesce((params->>'deathtouch')::boolean,false)` — keeps the primitive the single owner of creature mutation, and any future deathtouch-source burn reuses it). `apply_fight` captures BOTH creatures' `card_has_deathtouch` UP FRONT (alongside the powers, since fight is simultaneous — a creature that dies still deals its deathtouch hit) and passes the flag per hit. **`card_has_deathtouch` works inside `apply_fight`** despite its `is_session_player(session,auth.uid())` WHERE-guard, because resolve runs with a valid session-player `auth.uid()` (same as combat, which already uses it). **Latent-bug caught (bug-205):** F1/F5/F6 had asserted a 4/4 SURVIVES fighting a Deathtouch Viper (1/1) — wrong under CR 702.2 once deathtouch works (1 deathtouch dmg is lethal). Those tests had used the viper as a plain 1/1; swapped them to `Silhana Ledgewalker Test` (1/1, reach, NO deathtouch, NO ETB trigger — the go-to "vanilla 1/1 for fight tests"). Added F9 (deathtouch viper kills a 0/4 `Vengeful Wall Test` via 1 lethal dmg, survives since wall has 0 power) + F10 (deathtouch defender kills the 4/4 attacker too). **(2) Generic-mana parity.** Realization: EVERY creature-spell client dispatch already passes `undefined`/null for genericPayment — surfacing a generic-mana picker is a SHARED app-wide UI feature, NOT fight-specific. The only fight-specific gap was that `cast_fight` didn't even ACCEPT a generic-payment param (every other cast path threads `generic_payment` jsonb → `pay_mana_cost`). Fix: `cast_fight` gains `p_generic_payment jsonb default null`, forwarded to `pay_mana_cost`. **Signature gotcha:** adding a param changes the function identity, so `create or replace` can't do it — must `drop function if exists public.cast_fight(uuid,uuid,uuid,uuid,text)` first, then create the 6-arg version (`...,text,jsonb`); named-param rpc calls that omit `p_generic_payment` still resolve via the default. Client `castFight` wrapper gains `genericPayment?` forwarding (parity; dispatch passes undefined today like the others). **224/224, tsc/lint clean.** **Infra note (not a code bug):** mid-session the local DB started dropping external connections on 54322 ("Connection terminated unexpectedly") while the container reported healthy (33/100 conns, internal psql fine) — a Docker host↔container port-forward glitch; fixed by `docker restart supabase_db_LeylineSync` (NOT `supabase start`, which left the DB up and only restarted aux services). **fight is now done end-to-end: engine + spell cast + spell authoring + two-step picker + trigger path + trigger authoring + deathtouch + mana parity.**

- [2026-06-05] **`gain_control` COMPLETED (migration 107) — threaten extras + SPELL path; the 104/105 registry's first real payoff.** Two follow-ups from mig 106, both shipped: **(1) Threaten extras.** A real Threaten/Act of Treason also UNTAPS the stolen creature and gives it HASTE (else it's summoning-sick under the new controller and can't attack the turn you take it). gain_control now honours optional `untap`/`haste` booleans: untap clears is_tapped via a `case`-guarded UPDATE clause, haste inserts the same until-EOT 'haste' continuous-effect row grant_keyword uses (always until-EOT even for permanent control — haste matters the turn you gain it; next turn it's no longer sick). **(2) SPELL path** (Act of Treason is itself a sorcery, so this is the *canonical* shape, not the trigger). **This is where the 104/105 dispatch registry paid off concretely: NO reproduction of resolve_top_of_stack OR put_action_on_stack.** The whole spell path = (a) `gain_control_creature` added to the game_stack_items action_type CHECK, (b) one new builder `build_stack_payload_gain_control_creature` (creature_simple validation + echo duration/untap/haste), (c) handle_creature_effect reproduced from mig 104 with a one-line change — it now injects `acting_controller := p_stack_item.controller_player_id` into the params it passes to apply_creature_effect (mirrors the trigger seam from 106; behavior-neutral for every other creature kind), (d) one `stack_action_handlers` row (handler_fn=handle_creature_effect, builder_fn=the new builder). Contrast with how grant_keyword's spell path (mig 100, PRE-registry) needed verbatim reproduction of the ~320-line put_action_on_stack + resolve_top_of_stack — the registry erased that. **Spell path is FULLY harness-tested** (no client needed for verification): `putOnStack('gain_control_creature', {...})` → GC4 (EOT + untap + haste: caster controls, is_tapped=false, haste+control continuous-effect rows present, then expireEffects reverts control to owner and lapses both rows) + GC5 (opponent-only rejects your own creature at cast). **Client wiring** (mirrors grant_keyword mig 100): TargetedCreatureActionType += gain_control_creature, `putGainControlCreatureOnStack` wrapper (carries duration/untap/haste), getSpellPlan detects `gain_control` → a creature_effect plan carrying duration/untap/haste, the creatureEffect dispatch branches on `plan.effect === 'gain_control_creature'`. Authoring now BOTH contexts: registry `['trigger','spell']`, BuilderSpellEffect += gain_control, Zod gained optional untap/haste booleans, LLM prose updated (valid as trigger or instant/sorcery; untap/haste documented). Form models duration+target only; untap/haste are JSON/AI (consistent with not adding a boolean field kind). Added a `Scenario.playerId(seat)` accessor (was private) to assert the caster's id in the spell test. **228/228, tsc/lint clean.** gain_control is now COMPLETE: trigger + spell + threaten extras + authoring in both contexts. **Recipe confirmed: post-registry, adding a creature-targeting SPELL effect = CHECK value + 1 builder + 1 registry row (+ client wrapper/getSpellPlan); the two monster functions are never reproduced.**

- [2026-06-05] **Tier-2 `gain_control` (migration 106) — Threaten/Mind Control, TRIGGER path.** "When this enters, gain control of target creature you don't control [until end of turn]." **Key structural difference from every prior creature effect:** control is a DIRECT column (`game_cards.controller_player_id`), NOT a read-through continuous-effect layer like keywords/pump (which the `card_has_*`/`card_effective_power` accessors compute live). So the cleanup sweep — which only DELETEs continuous-effect rows — can't revert control by deletion. Design: permanent control = a direct UPDATE; until-end-of-turn = the UPDATE **plus** a `'control'` continuous-effect row carrying `{original_controller}`, and `expire_continuous_effects_for_step` gained an explicit RESTORE step (UPDATE controller back from the payload, same expiry predicate as the DELETE, guarded to battlefield-only) that runs BEFORE the delete. **Threading the acting controller:** `apply_creature_effect(session,kind,target,params)` has no controller param; rather than change its signature (and all call sites), `apply_targeted_triggered_ability_effects` now injects `acting_controller := p_controller_id` into the effect params jsonb it passes to apply_creature_effect — behavior-NEUTRAL for every other kind (they ignore the extra key). gain_control reads `acting_controller` (raises if absent) + `duration` ('permanent'default|'end_of_turn'). Routed through the creature-target picker by adding 'gain_control' to `trigger_effect_requires_creature_target` (+ target_type creature-only). effect_type CHECK += 'control'. **Reproduced functions** (gain_control is a primitive branch, NOT helped by the 104/105 dispatch registry — that was for resolve/put, not apply_creature_effect): apply_creature_effect (from 103), trigger_effect_requires_creature_target + apply_targeted_triggered_ability_effects (from 102), expire_continuous_effects_for_step (from baseline). Authoring: registry entry (duration enum + creatureTargetField, contexts ['trigger']), Zod member + KNOWN_V2, BuilderEffect union (trigger-only, NOT BuilderSpellEffect), LLM prose. Fixtures `Usurp Brute Test` (EOT) + `Dominate Beast Test` (permanent). Tests GC1 (EOT flips then reverts via expireEffects), GC2 (permanent doesn't revert), GC3 (controller restriction). **Test trick (no harness change):** assert control by comparing the taken creature's `controller_player_id` to the SOURCE creature's (= the acting seat) and to its own `owner_id` (= original) — both already on `cardState`, so no seat→playerId accessor needed. **226/226, tsc/lint clean.** **Deferred follow-ups:** (1) the "threaten" combat extras — untap + grant haste so the stolen creature can actually attack the turn you take it (without haste it's summoning-sick under the new controller); (2) the SPELL/instant path (`gain_control_creature` stack action — now CHEAP thanks to the 104/105 registry: a builder row echoing duration + handler_fn handle_creature_effect, but handle_creature_effect would need to inject acting_controller = stack_item.controller_player_id, mirroring the trigger seam). **Recipe note:** an effect that mutates a DIRECT column with an until-EOT duration needs a revert hook in expire_continuous_effects_for_step (the sweep is delete-only); read-through-layer effects (keywords/pump) need nothing there.

- [2026-06-05] **Cast-side dispatch is now DATA-DRIVEN too (migration 105) — companion to 104; kills the put_action_on_stack reproduction tax.** `put_action_on_stack` is the cast-side monster re-pasted per creature-targeting SPELL effect (migs 071/079/083/100 each reproduced its ~340 lines to add ONE action type to THREE spots: the whitelist `in(...)`, the per-action validation chain, and the payload-building `case`). 105 extracts each action's **validate-and-build-payload** into a builder of uniform signature `build_stack_payload_*(p_session_id uuid, p_actor uuid, p_payload jsonb, p_timing text, p_target_controller text) returns jsonb` (validates → RAISES on bad input exactly as before → returns the jsonb to store). Reuses the **same `stack_action_handlers` table** via a new nullable `builder_fn` column (null = not castable via put_action_on_stack: cast_permanent/modal_spell/scry/surveil/spell_effect/fight_creatures/triggered_ability use dedicated cast_* RPCs/enqueue). The whitelist `in(...)` is GONE — replaced by "the registry has a builder_fn for this action_type" (same 'Unsupported stack action type' error, same early position). The 5 simple creature actions (destroy/bounce/tap/untap/exile) share ONE `build_stack_payload_creature_simple` (identical validation + `{target_card_id,target_controller,timing}` payload). **Ordering-preservation care:** the validation chain originally ran AFTER the sorcery checks and BEFORE mana payment, so the single EXECUTE builder call is placed at that exact spot (raises before any mana paid); the counterspell-instant-timing guard and the sorcery guards STAY inline in the skeleton verbatim (they're action-specific but orthogonal to the reproduction tax, and moving them would change error precedence for malformed input). builder lookup happens early (whitelist position) but the builder CALL happens late (validation position) — split to preserve both the early unsupported-type error and the late validation order. counter_spell builder keeps its `for update` lock on the target item (locks persist for the tx regardless of which fn acquires them). Builders SECURITY DEFINER + revoked from public (internal, reached only via skeleton EXECUTE as owner). Pure behavior-preserving (no client/TS change): 224/224 unchanged. **NEW FULL RECIPE — a creature-targeting SPELL effect now needs ZERO function reproduction: (1) add the action_type to the game_stack_items CHECK, (2) INSERT/UPDATE a stack_action_handlers row with builder_fn (often the shared creature_simple) + handler_fn (often handle_creature_effect), (3) client wrapper. Both monster dispatchers (resolve + put) are now table-driven.** Remaining per-effect SQL is only genuinely-new mechanics (a new apply_creature_effect kind, or a non-creature_simple payload shape).

- [2026-06-05] **Stack-action dispatch is now DATA-DRIVEN (migration 104) — the registry-dispatcher refactor; kills the resolve_top_of_stack reproduction tax.** Every migration 087-103 that added a stack action type had to re-paste the entire ~210-line `resolve_top_of_stack` just to add ONE branch to its `action_type` if/elsif chain (superlinear copy-to-diff cost). 104 removes that: a `stack_action_handlers(action_type pk, handler_fn, description)` lookup table + each old branch lifted VERBATIM into a handler fn of a **uniform signature** `handle_*(p_session_id uuid, p_stack_item public.game_stack_items) returns jsonb`. **Handler contract:** return non-null jsonb → dispatcher returns it DIRECTLY (an awaiting_decision park; item NOT finalized); return null → dispatcher calls `finalize_stack_resolution`. This captures BOTH old branch shapes (side-effect-then-fallthrough-to-finalize = return null; park-for-decision = return the awaiting jsonb). scry/surveil's no-options case returns null (resolver finalizes) — same as the old `return finalize_stack_resolution(...)`. The 9 creature-effect action types (deal_damage/pump/destroy/exile/bounce/tap/untap/add_counters/grant_keyword `_creature`) all map to ONE `handle_creature_effect` (it still strips `_creature` → apply_creature_effect kind); scry+surveil share `handle_scry_surveil`. Dispatcher = auth/session-lock/top-item-select (unchanged) then `select handler_fn from stack_action_handlers` + `execute format('select public.%I($1,$2)', v_handler_fn) into v_result using p_session_id, v_stack_item`. **EXECUTE/composite mechanics that worked:** (a) `%I` quotes the table-controlled, public-schema handler name (no injection); the unqualified name is stored and `public.` prepended in the format string (can't `%I` a schema-qualified name — it'd quote the dot). (b) the whole `game_stack_items` ROWTYPE var passes fine as a bound `using` param to a composite-typed handler param. **Security:** handlers are SECURITY DEFINER/search_path=public (matching apply_fight et al.), reached ONLY via the dispatcher's EXECUTE which runs as the function owner — so NO grant to authenticated, and I `revoke all ... from public` on each (Postgres auto-grants EXECUTE to PUBLIC on creation) so handle_cast_permanent/handle_counter_spell etc. are NOT exposed as raw PostgREST RPCs. **auth.uid() nesting check:** none of the lifted branch bodies read auth.uid() (the single auth check stays in the dispatcher), so definer nesting is behavior-neutral; functions that read it deeper (card_has_deathtouch via apply_fight, apply_trigger_effects) still work because auth.uid() reads a session GUC unaffected by definer nesting. **NEW RECIPE — adding a stack action type is now: (1) INSERT one row into stack_action_handlers, (2) create one handle_* fn (+ revoke from public). resolve_top_of_stack is NEVER reproduced again.** Pure behavior-preserving refactor (no client/TS change): 224/224 unchanged, tsc/lint clean. Scoped first slice of the broader dispatch-table idea; `put_action_on_stack` (the OTHER monster that gets re-pasted per creature-targeting SPELL effect) is the next candidate for the same treatment but was left out to keep this diff minimal and verifiable.

- [2026-06-04] **Targeted single-creature effects in the form, via a composite `target` field kind.** Added `destroy`/`exile`/`bounce`/`tap`/`untap`/`pump` as registry entries (contexts trigger+spell). Engine contract confirmed from fixtures (Doom Blade, Banishing Bolt, Banisher Priest, Battlegrowth, Giant Growth): targeted effects use an **inline `target_type` (+ optional `target_controller`) on the action** — NOT the `targets[]`/`target_ref` wiring (scripts using target_ref stay JSON-mode; not modeled). New `target` FieldDescriptor kind: stored in-memory as a single option `value` (e.g. `creature_opponent`) but it OWNS two JSON keys — `effectToJson` expands it to `target_type`(+`target_controller`), `effectFromJson`/`parseFields` collapse the two keys back by matching options (controller vocab bucketed any/opponent/you via `controllerBucket`; `you`/`controller`/`self`→you). Strict-key allow-set special-cases target to permit target_type/target_controller. Combined dropdown chosen over two separate dropdowns (cleaner JSON: omits target_controller when unrestricted, e.g. Doom Blade = `{type:destroy,target_type:creature}`) — user picked it. Form UI needed ZERO changes: the existing `FieldControl` select branch renders any field with `options`. **Scope deliberately limited to always-creature-targeted effects.** Deferred: targeted `deal_damage` (Lightning Bolt) and targeted `add_counters` — those `type`s already have auto-resolve forms (recipient / on-self) in the registry, so a targeted variant needs a per-type disambiguator (registry is one-entry-per-`type`); a targeted add_counters/deal_damage script correctly stays JSON-mode for now. 121 tests green. This closes the main remaining form capability gap; the dual-shape variant mechanism is the next item if Lightning-Bolt-class cards are wanted in the form.
- [2026-06-04] **Quick-wins pass: spell-context decision effects + empty-field omission + mill.** (1) Enabled `discard`/`may`/`choose_player`/`search_library` as **spell effects** (contexts now `['trigger','spell']`) and added **`mill`** as a new registry entry (`['trigger','spell']`, fields amount+recipient). Verified safe in the engine first: `resolve_top_of_stack` runs `spell_effect` programs through the SAME `apply_trigger_effects` as triggers (migration 094 L552-553), whose `else` branch routes auto-resolve effects (incl. mill) to `apply_targeted_triggered_ability_effects` — so any effect that function handles works in both contexts. (2) **Empty-field omission:** optional `text`/`object`/`effect-list` fields that carry no info are now dropped from serialized JSON (`isEmptyValue` helper + skip in `effectToJson`) — so a no-filter tutor saves `{search_library,count,to}` not `…filter:{type_line:''}` (fixes the in-game "Search your library for a " prompt; also drops empty `may` prompts). Scalars (number/enum/select) are never "empty" — they keep materializing their default (recipient→each_opponent etc.), so step-1 behavior is unchanged. Round-trips stay stable because the in-memory form re-materializes the optional default on parse. (3) Fixed a real serialization bug en route (bug-174): spell actions were hardcoded as `{type,amount}`, dropping count/to/filter — now go through `registryEffectToJson`. Also updated the AI-guide prose (`card-behavior-llm.ts`) spell section to list the newly-valid spell actions. 106 tests green. Note: `BuilderEffect`/`BuilderSpellEffect` hand-unions are now ~duplicates of the registry — a future cleanup could type them structurally as `RegistryEffect`, but extending the unions was the minimal diff. Still NOT done: targeted effects in the form (destroy/exile/bounce/tap/untap/pump + targeted deal_damage/add_counters need a `target` field kind + the `targets[]` array) — the next real capability gap.
- [2026-06-04] **Step 2: four decision effects + recursive field kinds added to the registry.** Added `search_library`, `discard`, `may`, `choose_player` as `EFFECT_REGISTRY` entries (`['trigger']` context only — matches the fixtures; spell-context tutors are a one-line add later). Introduced three new `FieldDescriptor` kinds: `text` (free string, e.g. may.prompt), `object` (nested record with its own `fields`, parsed with strict sub-keys — for search_library's `filter.type_line`), and `effect-list` (a recursive array of effects parsed in an `itemContext` — for may/choose_player's `effects`). `effectDefault`/`effectToJson`/`effectFromJson` now dispatch per kind via mutually-recursive `fieldDefault`/`fieldToJson`/`parseFieldValue`+`parseFields` helpers; the form renderer gained `text`/`object` branches in `FieldControl` plus a recursive `EffectListControl` that reuses a now-generic `EffectEditor(context)` (so nested rows recurse back through `EffectFields`). Optional-default materialization is consistent with step 1 (a tutor without `filter`/`to` round-trips with the defaults filled). `effect-list` is always required (absent `effects` → JSON mode). The AI guide picked the four up automatically (it derives from `BUILDER_EFFECT_TYPES`). 92 characterization tests green (was 82), tsc+lint clean. Chose to build the `object`/`effect-list` primitives properly rather than special-case the filter, because they're reusable for the still-growing vocabulary (the whole point of step 1).
- [2026-06-04] **Card-behavior guided form becomes registry-driven (the engine vocabulary will grow a lot).** Replaced the per-effect hand-coded switches (`defaultEffect`, `effectToJson`, `parseEffects`, `parseSpellEffect`) and per-type form JSX with one declarative `EFFECT_REGISTRY` (`lib/game/card-behavior-registry.ts`): each effect type declares `{type,label,contexts,fields}` once, and defaults / JSON serialize / strict JSON parse / form widgets all derive from it. Form rendering is now a generic `<EffectFields>`/`<FieldControl>` walk over `fields` (number/enum/select kinds). `BUILDER_EFFECT_TYPES`/`BUILDER_SPELL_EFFECT_TYPES`/`BUILDER_RECIPIENTS`/`BUILDER_TOKEN_NAMES` are re-exported from the registry so the AI guide (`card-behavior-llm.ts`) stays in sync from the same source. **Deliberately NOT done:** generate the form from the Zod schema (schema is a loose validation contract, not a UI spec — kept as a separate source: schema = "is it valid", registry = "how to edit/teach"); couple schema to registry; or fold activated abilities into the registry (different cost+effect shape, left hand-coded). Adding a new form-editable effect = one registry entry (+ migration + Zod entry). Step 1 of 2: this is a pure refactor reproducing current capability; step 2 adds search_library/discard/may/choose_player (needs a recursive `effect-list` field for the nested ones). Chosen over (a) freezing the form and leaning on AI/JSON forever, and (b) a read-only structured preview — because the user confirmed the vocabulary keeps growing, so only schema-from-one-source scales.
- [2026-06-04] **Unified the form parser to STRICT (was asymmetric).** Found the two JSON→form parsers disagreed: `parseSpellEffect` was strict (unknown key / missing field → JSON mode) but `parseEffects` (trigger effects) was lenient — it ignored unknown keys (silently dropping them on save) and defaulted a missing `amount` to 0. The registry needs one rule, so unified to strict for both contexts: unknown key, missing required field, or invalid enum value → JSON mode; optional fields (recipient→each_opponent, create_token.count→1) keep defaults. Behavior change: a few malformed-but-previously-lenient trigger scripts now open in JSON instead of Form (no more silent data loss). Locked by characterization tests (`tests/unit/card-behavior-builder.test.ts`) written BEFORE the refactor — 82 cases pinning the form/JSON classification, form round-trip stability, and default-factory shapes. Chosen over faithfully reproducing the asymmetry (would bake latent bugs into the clean registry). User approved.
- [2026-06-02] **Local bootstrap split out of the push path (so `supabase db push` is safe for hosted).** The squash baseline + local-only relax-FK migration were moved OUT of `supabase/migrations/` into `supabase/local-bootstrap/` (`00_baseline.sql`, `01_relax_fks.sql`). `supabase/migrations/` now holds ONLY the real incrementals (087+) — that folder is the `db push` payload, and the bootstrap must never be in it (baseline would collide with the existing hosted schema; relax-FK would drop prod FKs). Consequence: `supabase db reset`/`migration up` can no longer build local (the incrementals assume base tables exist) — local is rebuilt by **`npm run test:db:setup`** (`scripts/setup-local-test-db.mjs`: drops `public`, applies bootstrap then migrations via a direct `pg` connection). Hosted deploy = `supabase link` + `supabase db push` (use `supabase migration list` to see the diff; `migration repair --status applied <ver>` to mark 087/088 done if already hand-applied). Chosen over Option B (reconcile-only via repair, leaves relax-FK a footgun in the push path) and Option A (keep pasting SQL manually). Replaces the earlier note that the relax-FK lived at `supabase/migrations/00000000000001_local_test_relax_fks.sql` — that path no longer exists.
- [2026-06-02] **Local DB = squashed dump baseline, not migration replay.** Discovered the incremental migration chain is not replayable from scratch (base tables cards/game_cards/game_players/profiles created out-of-band on hosted → bug-104; AND multiple returns-table functions change shape via create-or-replace → 42P13; AND Supabase's leading-version-digit keying makes mid-chain drop-shims impossible). Since hosted is applied MANUALLY (migrations/ isn't the prod driver), chose to squash for local: `supabase/migrations/` = one full-schema dump baseline + a local-only relax-FK migration; the 88 incrementals archived in `supabase/migrations_archive/` as history. New migrations still go in migrations/ and get pasted to hosted manually as before. Rejected: surgical per-table baseline (chain still didn't replay), drop-shim injection (version-PK collisions), out-of-band load (bespoke, db reset wouldn't work).
- [2026-06-02] **Test chamber = programmatic harness first (Option A), against local Supabase.** Chose a code-driven regression suite (drives real RPCs, asserts state) over an interactive in-app sandbox (Option B) as the first build — higher leverage (CI-able, protects the 084–086 refactors, encodes docs/test-plan-079-086.md permanently), and its scenario helpers (`place`/`cast`/`resolveStack`/`snapshot`) are the foundation a sandbox would later reuse, so it isn't throwaway. DB target: **local Supabase via Docker** over bare-Postgres-with-auth-shims or a hosted test project — the project is already supabase-initialized (config.toml) and Docker is installed; full prod fidelity (`db reset` replays the exact migrations), fast, offline. Cost accepted: tests need Docker Desktop running. Implementation keystone: direct `pg` + `request.jwt.claims` to satisfy `auth.uid()` (see Key Learnings). First slice = proving trio (R1/R8/R11) to validate the pipeline before expanding.
- [2026-06-01] **Rules engine stays in Postgres; refactor over rewrite.** Considered moving rules resolution to TypeScript (app code) vs keeping plpgsql. Chose: keep authority + atomicity + anti-cheat in Postgres (free transactional locking via `for update`, RLS, realtime), and instead pay down the duplication by centralizing in SQL — Phase 0 `put_in_graveyard` (done, migration 084), then `apply_effect` (one effect switch shared by spells/abilities/triggers) and `effective_characteristics` (one face/script accessor, also unlocks DFCs like Jill // Shiva). A pure-TS `reduce(state, action)` rules core is deferred to the **replacement-effect / layer-system frontier** (Noctis-class: "cast from graveyard" static permission, additional costs, finality counter = "if it would die, exile instead"), where SQL genuinely hurts. Rationale for eventually going TS there: shared client/server code → optimistic UI, and deterministic replay/undo for judge tooling. Rejected a full rewrite now: high risk for a solo project, and centralization makes either future cheap. Staging: Phase 0 centralizations → Phase 1 generic "pending decision" state machine (modal/scry/random, extends the existing `choose_*` pattern) → Phase 2 TS core only when replacement/layers arrive.
